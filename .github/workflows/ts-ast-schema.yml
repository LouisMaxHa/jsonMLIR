name: ts-ast schema

on:
  push:
    paths:
      - 'src/jsonmlir/**'
      - 'scripts/generate_ts_ast.py'
      - 'ts-ast/**'
      - 'tests/test_ts_ast_schema.py'
  pull_request:
    paths:
      - 'src/jsonmlir/**'
      - 'scripts/generate_ts_ast.py'
      - 'ts-ast/**'
      - 'tests/test_ts_ast_schema.py'

jobs:
  schema:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Python deps
        run: pip install pydantic pytest

      - name: Install ts-ast deps
        run: cd ts-ast && npm install

      - name: Regenerate ts-ast and check diff
        run: |
          python scripts/generate_ts_ast.py
          git diff --exit-code ts-ast/schema ts-ast/generated

      - name: Run schema tests
        run: PYTHONPATH=src pytest tests/test_ts_ast_schema.py -v
